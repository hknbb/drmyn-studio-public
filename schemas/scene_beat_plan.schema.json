{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://example.org/schemas/scene_beat_plan.schema.json",
  "title": "Scene Beat Plan",
  "description": "Source-beat semantic plan layer for a scene. Describes the fine-grained narrative/semantic units (beats) within a scene, independent of final Kling shot durations. This is the input layer to the omni_clip_planner, which computes Kling-valid integer-second shots (3..15s) from these source beats. Schema version 0.x-draft; promoted to 1.0 after A3 feedback loop closes.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "record_type",
    "scene_id",
    "source_beats",
    "provenance"
  ],
  "properties": {
    "schema_version": {
      "type": "string",
      "const": "0.x-draft",
      "description": "Pre-1.0 draft. Promoted to 1.0 after A3 feedback loop closes."
    },
    "record_type": {
      "type": "string",
      "const": "scene_beat_plan"
    },
    "scene_id": {
      "type": "string",
      "pattern": "^SC\\d{4}$",
      "description": "Scene identifier (e.g., SC0001)."
    },
    "source_beats": {
      "type": "array",
      "minItems": 1,
      "description": "Array of semantic source beats describing the scene's narrative/visual progression. Each beat is a fine-grained unit that may later be merged into Kling-valid shots by omni_clip_planner. Do NOT assign duration_seconds here; durations are computed by the packer during A5/B6.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "beat_id",
          "content",
          "semantic_duration_hint",
          "may_merge_with_next",
          "splittable"
        ],
        "properties": {
          "beat_id": {
            "type": "string",
            "pattern": "^[A-Z0-9_]+$",
            "minLength": 1,
            "maxLength": 50,
            "description": "Unique identifier for this beat within the scene (e.g., ESTABLISH_KITCHEN, DIALOGUE_NADIA_01, WRIST_INSERT_1)."
          },
          "content": {
            "type": "string",
            "minLength": 1,
            "maxLength": 2000,
            "description": "Human-readable description of what happens in this beat. Example: 'Pan across kitchen counter, settling on Nadia's hands as she pours espresso.'"
          },
          "semantic_duration_hint": {
            "type": "string",
            "enum": ["short_insert", "normal", "long_hold"],
            "description": "Semantic category guiding beat merging during packing: short_insert = sub-3s detail (eye movement, hand detail, object flourish); normal = standard beat (dialogue line, transition, action); long_hold = extended focus (establishing shot, meditation, sustained action). The packer uses this hint to decide merge-into-neighbor or standalone placement."
          },
          "may_merge_with_next": {
            "type": "boolean",
            "description": "True if this beat can be merged into the next beat during packing (preferred for short_insert and optional actions). False if this beat should preferably be its own shot or merged only as a last resort."
          },
          "splittable": {
            "type": "boolean",
            "description": "True if this beat can be split across multiple Kling shots during packing (e.g., a long sustained action or establishing shot). False if beat must remain atomic (e.g., dialogue punch-line, critical object reveal, narrative milestone)."
          },
          "narrative_role": {
            "type": "string",
            "enum": [
              "establish",
              "action",
              "dialogue",
              "hold",
              "transition",
              "pulse_check",
              "insert"
            ],
            "description": "Optional semantic role classification for the beat. establish = scene setting, context establishment; action = character physical action; dialogue = speech/character interaction; hold = sustained focus without action change; transition = location/subject shift; pulse_check = brief moment confirming emotional/thematic beat; insert = detail shot (hand, object, expression close-up)."
          },
          "required_element_ids": {
            "type": "array",
            "description": "Optional: repo element IDs (e.g. C01, LOC001, PROP003) that must be present or visible in this beat. Operator-authored; never computed or guessed from text. Used by packer to propagate to manifest shots. Resolves ambiguous pronouns (She/He/Her) at the manifest layer.",
            "items": {
              "type": "string",
              "pattern": "^[A-Z][A-Z0-9]*\\d+$",
              "minLength": 2,
              "maxLength": 20
            }
          },
          "figures": {
            "type": "array",
            "description": "Distinct on-screen figures present in this beat. Each figure carries its own Kling @alias so multiple figures sharing a base character identity are never collapsed (anti-clone). Propagated to omni_clip_manifest shots by the packer; operator-authored.",
            "items": {
              "type": "object",
              "additionalProperties": false,
              "required": ["figure_id", "base_element_id", "kling_alias", "role"],
              "properties": {
                "figure_id": { "type": "string", "pattern": "^FIG_[A-Z0-9_]+$" },
                "base_element_id": { "type": "string", "pattern": "^C\\d{2}$" },
                "kling_alias": { "type": "string", "pattern": "^@[A-Z0-9_]+$" },
                "role": { "type": "string", "minLength": 1, "maxLength": 160 },
                "distinguishing_detail": { "type": "string", "maxLength": 200 }
              }
            }
          },
          "subjects_off_frame": {
            "type": "array",
            "description": "Element IDs (C01, PROP001, etc.) that are physically in the scene but intentionally out of frame for this beat. Operator-authored acknowledgement; suppresses CONTINUITY_PRESENCE_GAP errors from validate_continuity_presence for the listed subjects. Required when a subject is present in surrounding beats but excluded here for genuine framing reasons (e.g. a tight insert that excludes a background figure).",
            "items": {
              "type": "string",
              "pattern": "^[A-Z][A-Z0-9]*\\d+$",
              "minLength": 2,
              "maxLength": 20
            }
          },
          "camera": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "angle": {
                "type": "string",
                "enum": [
                  "eye_level",
                  "low_angle",
                  "high_angle",
                  "over_shoulder",
                  "insert",
                  "dutch",
                  "top_down"
                ]
              },
              "framing": {
                "type": "string",
                "enum": [
                  "wide",
                  "medium_wide",
                  "medium",
                  "medium_close",
                  "close",
                  "extreme_close",
                  "insert"
                ]
              },
              "movement": {
                "type": "string",
                "enum": [
                  "static",
                  "pan",
                  "tilt",
                  "dolly_in",
                  "dolly_out",
                  "tracking",
                  "crane",
                  "handheld",
                  "whip_pan",
                  "rack_focus"
                ]
              },
              "lens_bias": {
                "type": "string",
                "maxLength": 80
              }
            }
          },
          "lighting": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "source": {
                "type": "string",
                "enum": [
                  "natural_daylight",
                  "filtered_daylight",
                  "practical",
                  "artificial",
                  "mixed",
                  "low_key",
                  "high_key"
                ]
              },
              "quality": {
                "type": "string",
                "enum": [
                  "soft",
                  "hard",
                  "diffused",
                  "directional"
                ]
              },
              "color_temp": {
                "type": "string",
                "enum": [
                  "warm",
                  "neutral",
                  "cool",
                  "mixed"
                ]
              },
              "intensity": {
                "type": "string",
                "enum": [
                  "dim",
                  "moderate",
                  "bright"
                ]
              }
            }
          },
          "motion": {
            "type": "object",
            "additionalProperties": false,
            "properties": {
              "subject_intensity": {
                "type": "number",
                "minimum": 0.0,
                "maximum": 1.0
              },
              "camera_intensity": {
                "type": "number",
                "minimum": 0.0,
                "maximum": 1.0
              },
              "blocking_notes": {
                "type": "string",
                "maxLength": 300
              }
            }
          },
          "coverage_role": {
            "type": "string",
            "enum": [
              "establish",
              "reaction",
              "insert",
              "reverse",
              "resolve",
              "push_in",
              "detail",
              "hold"
            ],
            "description": "Optional coverage grammar role hint for the director pass, propagated to the manifest shot. Lets a beat be covered by several distinct shots rather than one repeated framing."
          },
          "focus_alias": {
            "type": "string",
            "pattern": "^@[A-Z0-9_]+$",
            "description": "Optional: the figure/element @alias this beat's coverage is ON (its visual subject). Enables reverse/reaction/insert coverage."
          },
          "performance_note": {
            "type": "string",
            "maxLength": 300,
            "description": "Optional acting/emotion direction for the focus subject. Performance/emotion only - never new story facts. Woven into the shot's action prose by the renderer."
          },
          "diegetic_audio": {
            "type": "string",
            "maxLength": 200,
            "description": "Optional in-world sound-design cue, rendered as an 'Audio:' line. Non-speech only; spoken dialogue stays gated by native_audio_readiness."
          },
          "notes": {
            "type": "string",
            "maxLength": 1000,
            "description": "Optional human-readable notes, such as visual style cues, emotional intent, or special technical considerations."
          }
        }
      }
    },
    "notes": {
      "type": "string",
      "maxLength": 2000,
      "description": "Overall scene-level notes, such as mood, pacing constraints, or known dependencies on other scenes or elements."
    },
    "provenance": {
      "type": "object",
      "additionalProperties": false,
      "required": ["created_by", "created_at"],
      "properties": {
        "created_by": {
          "type": "string",
          "description": "User or agent that created this record."
        },
        "created_at": {
          "type": "string",
          "format": "date-time",
          "description": "ISO 8601 creation timestamp."
        }
      }
    }
  }
}
