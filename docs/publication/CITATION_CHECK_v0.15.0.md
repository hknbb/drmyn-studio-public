# Citation Check v0.15.0

Checked file: `CITATION.cff`

Result:
- File remains valid for current policy.
- No update applied in this hygiene PR because `v0.15.0` public snapshot DOI is not minted yet.

Action for publish step:
- After public snapshot release and DOI minting, update `CITATION.cff` and `.zenodo.json` in the public repo with the new DOI/version.
