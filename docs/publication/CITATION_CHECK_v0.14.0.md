# Citation Check v0.14.0

Checked file: `CITATION.cff`

Result:
- File is valid for current policy and points to public archived DOI/release (`v0.4.6`).
- No update applied in this hygiene PR because `v0.14.0` public snapshot DOI is not minted yet.

Action for publish step:
- After public snapshot release and DOI minting, update `CITATION.cff` and `.zenodo.json` in the public repo with new DOI/version.
